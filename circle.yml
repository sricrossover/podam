language: java
jdk:
  - oraclejdk8
  - oraclejdk7
script:
  - mvn clean verify
branches:
  only:
    - develop
test:
  post:
    - bundle exec rake custom:test:suite

after_success:
  - bash <(curl -s https://codecov.io/bash)
